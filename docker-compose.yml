services:
  db:
    image: postgres:17
    env_file:
      - .env
    ports:
      - "5435:5432"
    volumes:
      - jr_project_postgres_db_data:/var/lib/postgresql/data

volumes:
  jr_project_postgres_db_data: